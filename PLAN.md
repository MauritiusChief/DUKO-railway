# 用于 railway 的自动报价系统

* 最大化便利客户，客户可以自己拿自己系统的 quote 的 PDF/文本/截图直接发给员工，员工不用费事辨认，直接投入系统即可获取 SKU 清单
* 智能理解用户的意图，对客户混淆/模糊不清的地方推荐可能的 SKU，兜底可生成澄清邮件文本
* 自动获取客户想要的 SKU 哪些店里不卖，推荐可用的替代，兜底邮件
* 自动获取哪些货物缺货（需要实时连接 ERP），让 manager 决定调货，如果等不及也可推荐替代
* 自动填写报价，合并同类项以及调整顺序方便仓库取货，当前速度较慢（快速处理需要连接 ERP）
* 客户所需 SKU 清单存储在本地，如客户需批量调整 SKU 清单可直接在本地处理，省下手动一个个调整的麻烦
* 总处理时长减少 50%，提升报价正确率 30%（变相减少报价出错后再处理所浪费的时间），相当于减少了超过 50% 的人力
* 关键决策仍由 manager/customer service 把关，避免 AI 幻觉导致出错，进而导致口碑下降、客户流失

## resumo 计划

* Railway 云端部署，全美可访问，Volume 持久化数据库与向量索引
* 安全性：
  * JWT 多人认证（jsonwebtoken + bcryptjs，管理员创建账号，用户登录）
  * Helmet 安全响应头（CSP、HSTS、X-Frame-Options 等）
  * 速率限制（express-rate-limit：API 100次/15分钟，认证端点 10次/15分钟）
  * 输入验证与清理（zod 定义请求 schema，统一校验中间件）
* 仅暴露单端口，后端通过 `:xxxx/api/...` 访问，前端静态文件同端口提供
* 前端：Node.js TypeScript Vite React react-router-dom zustand
* 后端：Node.js TypeScript Express
* LLM API：DeepSeek（文本对话与工具调用）+ OpenRouter（多模态视觉）
* 数据库：
  * Exposed-Items → SQLite（better-sqlite3）替代 CSV，存储结构化字段
  * LanceDB 保留，用于 384 维向量语义嵌入搜索

## 设计

* 需要维护一个顶级产品列表，里面的有产品组合也有单一的产品
* 维护一个次级产品列表，里面全是单一产品，产品组合需要拆成单一产品
* 已有零件列表，单一产品需要拆解成零件

* 清洗原始零件列表
* 从洗过的零件列表生成颜色表格、单一零件表、产品表格
  * 颜色表：颜色码+描述里获取的字符串
  * 单一零件表：把斜线分割的共享零件拆开，比如 28B24/VSB24/VSD24-D 拆成 28B24-D, 28VSB24-D, 28VSD24-D 三行。然后每行都标注原始的零件和原始的描述
    - 单一零件表尽量让产品表认领符合直觉，因此需要善用白名单
  * 产品表：可从单一零件表生成的则填充，填充完之后额外添加特殊处理
    - 在使用此产品表之前，默认需要修复 B09F 后缀、冗余 -3 后缀、错误编码等问题
    - 换句话说，查找的产品一定是 DUKO 真实售卖的产品

* 首次输入时，是清单提取步骤。
  - 输入是文本形式的清单，可选的颜色 checkbox
  - 输出是 json 格式的可供前端解析的 1. 标准清单，2. 带候选的模糊清单 3. 找不到的未知清单
* 首次输入之后，出现表格，改表输入框和查询输入框
  * 表格可手动修改 1. 标准表在表中修改 2. 模糊表由用户选最佳匹配 3. 未知清单由用户手动填
  * 改表输入框用来输入改进指令，模型重新输出
    - 改表时，只获取最新的 json 三表，过往的用户指令写成单一 md 字符串
  * 查询输入框就是查询数据库功能
* 表格改满意之后，点击按钮即可生成零件清单
* 总共需要三个 agent：
  1. 初始解析 agent
  2. 改表 agent
  3. 回答 agent
