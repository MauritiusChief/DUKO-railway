# Agent Skills

本目录只保存程序性检查清单，不记录架构事实。触发后先阅读相应 `SKILL.md`，再按任务裁剪步骤：

| Skill | 触发条件 |
| --- | --- |
| `documentation-maintenance` | 新增、修改、删除文档；代码变化需要同步 README、Wiki 或 context |

多个条件可同时触发。Skill 不能替代 `AGENTS.md`、代码阅读、测试或安全审批。
