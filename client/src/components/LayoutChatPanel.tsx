/**
 * LayoutChatPanel —— 布局 AI 对话面板（占位）
 *
 * Phase 4 将实现完整的 Agent 对话功能，
 * 包括与布局工具交互、多轮编辑等。
 */
import './LayoutChatPanel.css';

export function LayoutChatPanel() {
  return (
    <div className="lc-panel">
      <div className="lc-panel-header">对话</div>
      <div className="lc-panel-placeholder">
        对话功能即将上线
      </div>
    </div>
  );
}
